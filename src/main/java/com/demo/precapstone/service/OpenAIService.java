package com.demo.precapstone.service;

import org.springframework.stereotype.Service;

@Service
public class OpenAIService {
    public String createPromptFromText(String MessageText) {
        // 문자를 받아서 프롬프트로 변경 후 반환하는 로직 구현
        return MessageText;
    }
}
